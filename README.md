This is a page of company lionmotorsgroup, make with Next.js

## Run in dev

1. Clone the repository.
2. Create a copy of `.env.template` and rename to `.env` and changes the environments vars.
3. Install dependecies `npm install`.
4. Up the database `docker compose up -d`.
5. Run the proyect `npm run dev`.
